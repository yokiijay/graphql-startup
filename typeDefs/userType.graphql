type User {
  id: ID!,
  name: String,
  age: Int
}

input UserInput {
  name: String,
  age: Int
}

type Query {
  allUsers: [User!]
  user(id: ID!): User
}

type Mutation {
  createUser(input: UserInput): User
  deleteUser(id: ID!): String
}