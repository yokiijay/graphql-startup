type Post {
  id: ID!
  title: String
  content: String
}

input PostInput {
  title: String!
  content: String!
}

type Query {
  PostList: [Post!]
  Post(id: ID!): Post
}

type Mutation {
  SavePost(input: PostInput!): Post!
}